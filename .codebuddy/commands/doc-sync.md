请阅读 `.codebuddy/skills/code-documentation/SKILL.md` 并严格遵循其中的代码自文档体系。

**务必遵守三条铁律：**
1. 每次回复先称呼 "Boss"
2. 不确定的设计问题必须先问 Boss
3. 不写兼容性代码，除非 Boss 主动要求

**你的任务是：重新分析代码，修正并同步三层代码自文档体系。**

这不是简单的"检查差异"——你需要**重新阅读源码**，验证文档内容是否准确。

执行步骤：

**第一步：确定同步范围**
- 如果指定了路径参数：只分析该路径下的文件和目录
- 如果没有路径参数：分析整个项目
- 检测项目版本控制系统（Git 或 SVN），查看最近变更作为辅助参考

**第二步：扫描并识别文件类型**
对范围内的每个文件：
1. 判断是否为自动生成代码（按 SKILL.md 的三种识别方式）
   - 如果是自动生成 → 跳过
   - 如果已有三行注释但文件是自动生成的 → 标记为"误加注释"
2. 判断是否为需要注释的源码文件
3. 判断目录是否需要 CONTEXT.md

**第三步：调用 code-explorer agent 重新分析源码（核心步骤）**
将范围内的文件交给 code-explorer agent 进行全量阅读：
1. agent 使用 list_files 列出所有源码文件
2. agent 使用 read_file **逐个完整阅读所有源码文件**（禁止抽样）
3. agent 从 import/require/include 语句中提取真实的 INPUT 依赖
4. agent 从 export/public 方法/对外接口中提取真实的 OUTPUT 能力
5. agent 根据目录结构和实际调用关系确认真实的 POS 定位
6. 基于 agent 返回的分析结果，与现有注释对比：
   - 🔴 **缺失**：文件没有头部注释
   - 🟡 **过时/不准确**：注释内容与实际代码不符
   - 🔵 **误加**：自动生成文件上有不该存在的注释
   - 🟢 **正常**：注释准确反映了当前代码

**第四步：重新分析模块文档**
对每个需要 CONTEXT.md 的目录：
1. **实际阅读目录下的文件**
2. 检查 CONTEXT.md 是否存在
3. 如果存在：验证"业务域清单"是否与实际文件匹配
4. 验证"地位""逻辑""约束"描述是否还准确
5. 标记状态：🔴 缺失 / 🟡 过时 / 🟢 正常

**第五步：向 Boss 汇报差异**（表格形式）

**第六步：等待 Boss 确认后批量更新**
- 确保幂等性：已有注释的文件更新内容而非重复添加

**第七步：汇报更新结果**

$ARGUMENTS
