请阅读以下技能并严格遵循：
1. `.codebuddy/rules/code-documentation.md`（三层代码自文档体系）
2. `.codebuddy/rules/gitnexus-code-intelligence.md`（GitNexus 代码智能）

**务必遵守三条铁律：**
1. 每次回复先称呼 "Boss"
2. 不确定的设计问题必须先问 Boss
3. 不写兼容性代码，除非 Boss 主动要求

**你的任务是：基于变更检测，精准同步三层代码自文档体系。**

**第零步：环境检查**
1. 执行 GitNexus 可用性检查
2. 若索引过期，则执行 `npx gitnexus analyze` 更新
3. 若不可用，则降级到原始全量扫描方案

**第一步：确定同步范围（智能增量）**
- 指定了路径 → 使用 **模式 A** 分析该路径下文件
- 未指定路径 → 使用 **模式 C**（变更影响分析）：
  1. 获取 git diff 变更文件列表
  2. 获取每个变更的完整影响范围（直接 + 间接依赖）
  3. 同步范围 = 变更文件 + 受影响的直接依赖文件

**第二步：文件类型过滤**
沿用原方案的自动生成代码识别逻辑（三种方式不变）

**第三步：通过 GitNexus 获取最新关系数据**
对同步范围内的每个文件，使用 **模式 A** 获取最新数据，与现有注释对比：
- 🔴 缺失 / 🟡 过时 / 🔵 误加 / 🟢 正常

**第四步：重新分析受影响模块的 CONTEXT.md**
仅对包含变更文件的目录，使用 **模式 B** 获取最新模块信息并对比

**第五步：AI 精准补充验证（仅针对 🟡 标记项）**

**第六步：向 Boss 汇报差异（表格形式）**

**第七步：等待 Boss 确认后批量更新**

**第八步：汇报更新结果**

$ARGUMENTS
