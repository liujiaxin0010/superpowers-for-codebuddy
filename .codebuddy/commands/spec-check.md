请按以下顺序阅读并严格遵循：
1. `.codebuddy/skills/spec-organization/SKILL.md`（规格文档组织）

**务必遵守三条铁律：**
1. 每次回复第一句话必须称呼 "Boss"
2. 遇到不确定设计必须先询问 Boss
3. 未经 Boss 明确要求，不得编写兼容性代码

**你的任务是：**
检查项目 `spec/` 目录是否符合三级结构规范，输出合规报告与整改建议；**整改需经 Boss 确认后才执行，不擅自移动/重命名文件。**

执行步骤：

1. 读取 `spec-organization/references/structure-spec.md` 的目录结构与命名规范
2. 扫描项目 `spec/` 目录树（无 `spec/` → 提示是否需 `spec-organization` 建立）
3. 按四项逐一检查：
   - **必须文件**：每个子组件/子模块目录是否有 README.md
   - **命名规范**：核心流程 `{业务名称}流程.md`、测试套 `{功能名称}测试套.md`
   - **目录层级**：文件是否放在正确层级，有无跨层错放
   - **可选文件**：技术选型/OpenAPI/数据库设计/配置设计 是否仅在涉及时创建
4. 同时核对唯一事实来源风险（规格分裂/文档私藏/代码即文档迹象）
5. 输出合规报告（表格）：合规项 / 不合规项（位置 + 问题 + 整改建议）
6. **等 Boss 确认后**再执行整改（创建缺失 README、重命名不规范文件等）

补充约束：
- 整改前先列清单，经 Boss 确认；不擅自移动/删除/重命名
- 子目录（核心流程/、测试用例/、配置设计/）本身不需要 README，不要误报
- 报告须包含「本次未改动任何文件」声明（确认阶段）

$ARGUMENTS
