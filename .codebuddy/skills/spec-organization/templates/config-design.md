# {子组件名称} 配置说明

> 配置需同时提供说明文档（本文件）与样例文件（同目录 `config.yaml`）。放 `spec/{子组件}/配置设计/`。

## 配置结构

主配置（全局行为）+ 分项配置（如每通道独立 YAML），实现关注点分离。

## 配置项说明（逐段 YAML 示例驱动）

### {配置段名称}

```yaml
{段名}:
  {key}: {value}    # 说明：{这一项干什么}
  {key2}: {value2}  # 说明：{取值范围 / 默认值 / 边界}
```

| 配置项 | 类型 | 默认值 | 必填 | 说明 |
|---|---|---|---|---|
| {key} | {类型} | {默认} | 是/否 | {作用 + 边界} |

## 最佳实践

| 场景 | 参数建议 |
|---|---|
| 高吞吐 | {批量大小/并发数建议} |
| 低资源 | {建议} |

## FAQ

- **Q: {常见问题}** — A: {解答}

## 完整样例

> 同目录 `config.yaml` 提供可直接复制修改的完整配置样例（配置即文档）。
