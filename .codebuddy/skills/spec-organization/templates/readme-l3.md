# {子模块名称}

> 子模块级 README（低层 Spec，面向 AI，作为代码生成的直接输入）。放 `spec/{子组件}/子模块/{子模块名}/README.md`。

## 概述

一段话：这个子模块干什么、输入输出是什么、边界条件是什么。

## 层次定位

```
子系统 > 子组件 > [本子模块] ← 本文档范围
```

## 功能清单

| 功能点 | 描述 | 输入 | 输出 | 边界条件 |
|---|---|---|---|---|
| {功能点} | {做什么} | {INPUT} | {OUTPUT} | {空值/越界/超时行为} |

## 负责 / 不负责

| 负责 | 不负责 |
|---|---|
| {本模块做什么} | {明确不做什么，如「Base64→[]float32 是服务层职责」} |

## 类划分

| 类 | 职责 |
|---|---|
| {类名} | {职责} |

## 文档索引

- 核心流程：`./核心流程/{业务名称}流程.md`
- 对外接口：{接口定义链接}

> 低层 Spec 锁死接口契约（入参/出参/错误码），但**不写具体函数名/变量名**——内部实现路径留白。
