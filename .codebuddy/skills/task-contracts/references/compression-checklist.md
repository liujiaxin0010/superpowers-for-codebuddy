# 合同压缩清单

将完整模板压缩成 agent 可执行摘要前，确认以下内容都存在：

- `任务目标` 可用一句话表达
- `允许修改` 是目录/文件边界，不是泛泛而谈
- `禁止修改` 明确到核心目录、接口、依赖或架构层
- `验证命令` 可实际运行，不是“请自测”
- `交付证据` 不是只交 diff
- `owner` 明确，不为空
- `超边界处理` 明确为“停下并回退”而不是“继续试试”
