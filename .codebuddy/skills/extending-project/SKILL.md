---
name: extending-project
description: 项目功能扩展技能。用于在已有项目上新增功能、接入新模块或扩展现有链路时，先理解架构与日志约束，再做影响评估、扩展模式选择、计划编排和安全接入，尽量通过加法式扩展实现高内聚、低耦合。用户提到“给现有项目加功能/extend/在已有系统上新增模块/往现有链路接新能力”时触发。
---

# 项目功能扩展

本技能回答的是：**在不破坏现有系统的前提下，新功能应该接在哪里、动哪些地方、哪些地方绝不能顺手改。**

## 资源加载规则

当需要按"项目阅读优先级"读取原实现时，再读取：

- `.codebuddy/rules/project-reading.md`（强制优先三层文档 + GitNexus）
- `.codebuddy/rules/gitnexus-code-intelligence.md`（含模式 G 基线刷新）

当生成"历史实现规格 spec"时，再读取：

- `templates/historical-spec-template.md`

当生成"扩展需求分析规格 spec"时，再读取：

- `templates/requirement-analysis-template.md`

当需要细化"影响评估 / 扩展模式 / 日志与观测接入 / 停手信号"时，再读取：

- `references/extension-phase-guide.md`

当需要把新增点、修改点、保留不动点和风险结论整理成统一输出时，再读取：

- `templates/impact-assessment-template.md`

不要一开始就把模板全部加载；先搞清架构与边界，再按需套用。

## 何时使用

1. 在已有项目上新增功能
2. 需要先判断是新增模块还是修改现有模块
3. 需要控制改动面、回归风险和日志一致性

## 何时不用

以下情况优先考虑其他技能：

1. 纯修 bug：优先 `bug-fix`
2. 纯重构：优先 `writing-plans` / `executing-plans`
3. 需求仍然模糊：优先 `brainstorming` 或 `spec-lite`

## 阻断条件

出现以下任一情况时，返回 `BLOCKED`：

1. 需求目标、范围或入口不明确
2. 无法确定当前项目的核心架构和日志约束
3. 需要大范围改共享契约，但 spec/plan 尚未明确
4. **未生成 `historical-spec.md` 或 Boss 未确认核实**（Issue 1 强约束）
5. **未生成 `requirement-analysis.md` 或未包含追溯矩阵 / Boss 未确认**（Issue 2 强约束）
6. **未按 project-reading.md 的"三层文档→GitNexus→手动"优先级执行，且未在 progress.md 声明降级原因**（Issue 3 强约束）

## 扩展协议

### 0. 强制四步前置（顺序不可逆，缺一阻断）

```text
[Step 0.1] 项目理解
  → 严格按 project-reading.md 的优先级：
    三层文档 → GitNexus（先做基线对比/刷新） → 手动阅读
  → 在 docs/progress.md 记录信息源与降级原因

[Step 0.2] 历史实现规格 spec（new！）
  → 把"原实现已经做了什么"反向沉淀成 spec
  → 路径：docs/specs/YYYY-MM-DD-<模块名>-historical-spec.md
  → 模板：templates/historical-spec-template.md
  → 内容必须详细到：业务目标、用例清单、对外接口、数据模型、
    关键流程（含 Mermaid）、跨模块依赖、当前缺陷与债务、
    日志/观测约束、隐含约束（注释/测试反推）
  → 强制提交给 Boss 核实是否有偏差，未确认前不得进入 Step 0.3

[Step 0.3] 头脑风暴（结合历史 spec + 扩展需求）
  → 触发 brainstorming/SKILL.md 完整七阶段
  → 必须把 historical-spec.md 作为输入，发散时显式列出
    "复用 / 扩展 / 替换 / 废弃" 四类决策

[Step 0.4] 扩展需求分析规格 spec（new！）
  → 路径：docs/specs/YYYY-MM-DD-<需求名称>-requirement-analysis.md
  → 模板：templates/requirement-analysis-template.md
  → 必须从 historical-spec.md 中逐条引用受影响项
  → 必须包含"需求 → 设计 → 实现 → 验证"四列追溯矩阵
  → 强制提交给 Boss 核实，未确认前不得进入 Step 1
```

**Step 0.2 与 Step 0.4 都要让 Boss 显式打勾确认；只要还有"待补充/TBD"就阻断。**

### 1. 先理解项目（Step 0 输出已覆盖此项）

至少确认：

1. 项目类型、技术栈、目录约定
2. 核心入口和依赖关系
3. 测试方式
4. 日志框架、字段结构、控制台输出边界

### 2. 再做影响评估

必须输出三类结论：

1. 必须新增的文件/模块
2. 必须修改的现有文件
3. 应保持不动的核心模块

### 3. 决定扩展模式

| 判断条件 | 扩展模式 | 典型操作 |
|---|---|---|
| 新功能与现有功能无数据共享 | 新增独立模块 | 新建目录 + 通过接口注册 |
| 新功能复用现有数据但独立展示 | 新增路由/页面 | 新建路由 + 复用现有 service |
| 新功能是现有功能的变体 | 新增处理器/组件 | 在现有模块目录下新增文件 |
| 必须修改现有行为才能实现 | 最小侵入式修改 | 仅改必要接口，标注风险等级 |

选择原则：加法优先，修改最小化。只有最后一种模式需要标注风险。

### 4. 再进入计划和执行

扩展任务在进入实现前，必须：

1. 明确“新增”与“修改”的边界
2. 每个修改类任务标注风险等级
3. 每步都保留回归验证命令

## 扩展设计原则

1. **高内聚**：新功能代码尽量集中在独立模块
2. **低耦合**：通过清晰接口与现有系统交互
3. **加法优先**：先新增，再接入，最后才考虑最小修改
4. **日志一致**：沿用现有日志结构，不引入中文日志和随意控制台输出

## 风险检查

扩展前后重点检查：

1. 是否改动了共享契约
2. 是否影响原有测试主路径
3. 是否引入循环依赖
4. 是否把临时逻辑散落进多个旧文件

## 持久化记录要求

每次执行 `/extend` 都必须：

1. 至少更新一次 `docs/progress.md`
2. 如有技术判断、分流决策或风险结论，同步更新 `docs/findings.md`
3. 若已完成影响评估，优先套用模板沉淀“新增 / 修改 / 不动 / 风险”四类结论

## 禁止事项

1. 不要在没搞清架构前直接改代码——不理解现有结构的扩展等于在暗室里砌墙
2. 不要把”扩展”写成对现有模块的大面积侵入式改造——侵入式改造是重构，不是扩展
3. 不要跳过回归验证——新增代码不出 bug 不代表旧代码没被影响
4. 不要忽略日志结构和观测约束——日志不一致会让排障时无法关联新旧模块
5. 不要把高风险共享修改伪装成”小改动”——改共享契约的影响面是所有消费者
6. 不要在扩展时引入循环依赖——新模块依赖旧模块可以，旧模块反向依赖新模块不行
