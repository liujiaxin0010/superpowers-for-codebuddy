# {特性/组件名称} 需求规格

> 重要设计变更（醒目标注）：{如「已移除桥接层 Write SDK，改为 Client 模式」}

## 开发背景

{为什么要做这个特性：业务背景、要解决的问题、所属系统位置}

## 功能描述（分层功能清单）

| 模块 | 功能点 | 描述 | 依赖 | 实现状态 |
|---|---|---|---|---|
| {模块} | {功能点} | {做什么} | {依赖的功能点/外部组件} | 📋 规划中 / 🚧 开发中 / ✅ 已实现 |

## 边界条件前置声明

| 场景 | 边界条件 | 处理策略 |
|---|---|---|
| {如 类型转换} | {如 string→int 转换失败} | {如 记录错误并跳过 / 中止批次} |
| {如 数据为空} | {空值/零值} | {行为} |
| {如 重试} | {重试上限/超时阈值} | {策略} |

## 接口规格前置（如涉及外部依赖）

```
{引用外部 SDK 接口定义，如 Go struct / 函数签名}
```

明确接口契约（入参/出参/错误码），避免设计阶段再补。

## 关键决策显式声明

| 决策项 | 选择 | 理由 |
|---|---|---|
| {如 写入模式} | {insert/update/upsert} | {为什么} |
| {如 并发策略} | {串行/并行/并行度} | {为什么} |
| {如 错误传播} | {向上抛/就地处理/熔断} | {为什么} |

## 已移除的功能

| 功能 | 移除原因 | 替代方案 |
|---|---|---|
| {功能} | {为什么移除} | {用什么替代} |
