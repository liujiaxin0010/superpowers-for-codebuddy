{
  "_comment": "写完即检 hook 配置样例（TEMPLATE_VERSION 1.0.0）。合入业务项目的 .claude/settings.json（或 CodeBuddy 等价设置文件）的 hooks 字段；已有 hooks 则合并 PostToolUse 数组。字段名以所用 CLI 的 hooks 文档为准。检查失败时脚本退出码 2、stderr 回馈给 AI 当场修正；环境缺 bash/python 等工具时对应类型自动跳过，不挡路。",
  "_templateVersion": "1.0.0",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node .codebuddy/skills/instant-check/scripts/post-write-check.js"
          }
        ]
      }
    ]
  }
}
